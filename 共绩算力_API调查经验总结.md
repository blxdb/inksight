# 共绩算力 (suanli.cn) — API 调查经验总结

> 记录时间：2026-06-12
> 目标：查询四川三区 Job批处理 抢占式实例的余量

---

## 一、平台基本信息

| 项目 | 内容 |
|------|------|
| 平台名 | 共绩算力 |
| 官网 | https://suanli.cn |
| 控制台 | https://console.suanli.cn |
| OpenAPI | https://openapi.suanli.cn |
| 登录手机号 | 13568944601 |
| 登录密码 | tanabaM49! |

## 二、API 密钥与鉴权

文件中的 API 密钥属于 **快速模式**（Token 直传，无需签名）：

```
密钥: 84c7872c-c821-4add-8834-4cc224bc7cfb-20260612165549
      ↑ UUID                                  ↑ 过期时间 2026-06-12 16:55:49
```

所有 API 请求 Header 必须带：

```json
{
  "token": "84c7872c-...",
  "timestamp": "1740000000",
  "version": "1.2.0"
}
```

快速模式直接传 Token 即可，无需 RSA 签名。
（浏览器登录后的会话用的是 RSA 签名模式，需要 sign_str 字段）

## 三、正确的产品：Job批处理 + 抢占式实例

**不是** 云主机，**不是** 弹性部署服务。

正确路径：控制台 → **Job批处理** → **新增Job批处理**
页面 URL：`https://console.suanli.cn/job/create`

产品特征：
- 名为"抢占式实例"，标注"大幅折扣"
- 按秒计费
- 适合容忍被中断的批处理任务

## 四、正确的 API 调用（最关键的发现）

### ❌ 错误的调用（一开始犯的错）

```
GET /api/deployment/resource/search
```
→ 查到的是 **全价资源**，价格高了 ~5 倍

### ✅ 正确的调用

```
GET /api/deployment/resource/search?device_type=GpuDevice&sub_type=Spot&task_type=Job
```

| 参数 | 值 | 含义 |
|------|----|------|
| `device_type` | `GpuDevice` | GPU 设备 |
| `sub_type` | `Spot` | **抢占式实例**（最关键！） |
| `task_type` | `Job` | Job批处理 |

这两个参数 `sub_type=Spot&task_type=Job` 是**从浏览器控制台的 Network 请求里抓到的**。

## 五、价格单位（最大的坑）

API 返回的 `price` / `discount_price` 字段，单位是 **微元/秒**（0.000001 元/秒）。

**不是** 分/小时，**不是** 元/小时。

### 正确换算公式

```python
# 微元/秒 → 元/小时
yuan_per_hour = price * 0.000001 * 3600
# 简化：price * 0.0036
```

### 价格对比示例

| 区域 | 机型 | 原始 price | 元/秒 | 元/小时 |
|------|------|-----------|-------|---------|
| 四川三区 | 4090-48G 1卡 | 503 | 0.000503 | **¥1.81** |
| 四川三区 | 4090-48G 2卡 | 1006 | 0.001006 | **¥3.62** |
| 四川三区 | 4090-48G 4卡 | 2012 | 0.002012 | **¥7.24** |
| 四川三区 | 4090-48G 8卡 | 4024 | 0.004024 | **¥14.49** |
| 河南二区 | 4090(24G) 1卡 | 331 | 0.000331 | **¥1.19** |

注意：4090-48G 是 48GB 显存版本，4090(24G) 是 24GB 标准版。

## 六、inventory 字段含义

| API 返回值 | 控制台显示 |
|-----------|-----------|
| `inventory: 0` | "库存：紧张" （**不是**售罄） |
| `inventory: 2` | "库存：紧张" |

所以 `inventory` 的值很小（0/1/2 级别），更多是 **库存状态编码**，不是精确的剩余台数。

## 七、可用区域（抢占式 Job批处理）

控制台区域下拉框可选：

| 区域 ID | 显示名称 |
|---------|---------|
| henan-xc-p1 | 河南二区 |
| shsq-p1 | 上海三区 |
| **scws-p1** | **四川三区**（用户目标） |
| tj1q | 天津一区 |
| zjsx-p1 | 浙江四区 |

## 八、创建任务的完整步骤

### 先决条件：创建队列
1. 进入「队列列表」→ 点击「创建队列」
2. 填写队列名称、并发上限 → 提交

### 创建任务
1. 进入「新增Job批处理」页面
2. 选区域（点击下拉框→点选区域）
3. 选 GPU 型号（点击 GPU 卡片——光选区域不会自动选中！）
4. ✅ 确认"抢占式实例"被选中（默认就是）
5. 填写镜像 URL（**必须用私有仓库或平台镜像仓库的链接**，公共 docker hub 镜像如 `python:3.11-alpine` 会被拒）
6. 选队列（刚创建的）
7. 填"组内任务总数"=1，"组内并发任务上限"=1
8. 改"单元运行时长"为短时间（如 1 分钟）
9. 勾选服务协议
10. 点击「部署服务」

### 镜像说明
- 不支持公共 docker hub 镜像（返回"无效的镜像格式"）
- 必须用 `harborpush.suanleme.cn/用户名/镜像名:标签` 格式
- 或者先上传到平台的镜像仓库

## 九、监控脚本

脚本位置：`桌面\监控ADB连接.py`
（原脚本是监控 ADB 设备连接的，后来把四川三区余量监控合并了进去）

功能：
- 每 30 分钟查一次共绩算力 API
- 只关注四川三区 (scws-p1) 的抢占式实例余量
- 有余量时在桌面生成 `四川三区有余量_时间.txt` 通知文件
- 同时监控 ADB 设备在线状态和 Syncthing 进程

脚本中使用的 API 查询（可直接 curl 测试）：

```bash
curl -s -H "token: 84c7872c-c821-4add-8834-4cc224bc7cfb-20260612165549" ^
  -H "timestamp: 1740000000" ^
  -H "version: 1.2.0" ^
  "https://openapi.suanli.cn/api/deployment/resource/search?device_type=GpuDevice&sub_type=Spot&task_type=Job"
```

## 十、浏览器操作踩坑记录

1. **登录**：页面默认是"手机登录/注册"tab，要手动切到"密码登录"tab
2. **双击登录**：第一次点登录没反应是因为协议复选框没勾，需要先点"我已阅读并同意"
3. **选区域**：下拉框是 `[role="combobox"]`，展开后选项是 `[role="option"]`，用 playwright 的 `locator('[role="option"]')` 查找含目标文本的元素
4. **选 GPU**：选完区域不会自动选中 GPU，必须再点一下 GPU 卡片（heading 或卡片区域）
5. **弹窗元素定位**：该平台用 `[ref=xxx]` 属性作为标识，但这些 ref 每次页面加载会变，不能用硬编码，要用文本内容或 role 定位
6. **创建任务**：需填多项参数，容易漏掉"组内任务总数"和"组内并发任务上限"
7. **镜像格式**：docker hub 公共镜像被拒绝，必须用私有仓库地址

---

*本文件是纯文档，不会影响任何 AI 会话的上下文。需要时打开阅读即可。*
